import 'package:flutter/material.dart';

class Screen06 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen 06')),
      body: Center(
        child:
            Text('Screen 06 — Placeholder for Figma design (node-id: 15-297)'),
      ),
    );
  }
}
