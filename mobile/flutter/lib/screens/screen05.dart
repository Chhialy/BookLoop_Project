import 'package:flutter/material.dart';

class Screen05 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen 05')),
      body: Center(
        child:
            Text('Screen 05 — Placeholder for Figma design (node-id: 15-921)'),
      ),
    );
  }
}
