import 'package:flutter/material.dart';

class Screen04 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen 04')),
      body: Center(
        child:
            Text('Screen 04 — Placeholder for Figma design (node-id: 15-809)'),
      ),
    );
  }
}
