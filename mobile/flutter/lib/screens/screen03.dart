import 'package:flutter/material.dart';

class Screen03 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen 03')),
      body: Center(
        child:
            Text('Screen 03 — Placeholder for Figma design (node-id: 15-1025)'),
      ),
    );
  }
}
